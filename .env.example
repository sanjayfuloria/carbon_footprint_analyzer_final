# Example environment file for Carbon Footprint Analyzer
# Copy to .env and replace placeholders with your real keys.
# Never commit your real secrets.

# OpenAI API Key (required for LLM extraction)
OPENAI_API_KEY=YOUR_OPENAI_API_KEY

# Groq API Key (optional; enable if you select Groq in the UI)
GROQ_API_KEY=YOUR_GROQ_API_KEY

# LangSmith / LangChain tracing (optional)
LANGCHAIN_TRACING_V2=true
LANGCHAIN_API_KEY=YOUR_LANGSMITH_API_KEY
LANGCHAIN_PROJECT=carbon-footprint-analyzer
LANGCHAIN_ENDPOINT=https://api.smith.langchain.com

# Notes:
# - Do NOT use real token prefixes (e.g., sk- or gsk_) in this example file to avoid push protection flags.
# - Set only OPENAI_API_KEY if you are not using Groq.
